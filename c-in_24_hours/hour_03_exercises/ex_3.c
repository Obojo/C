/**
 * What is wrong with the following function?
 */

int integer_add( int x, int y, int z)
{
	int sum;
	
	sum = x + y + z
	return sum;
}

/* The "sum = x + y + z" is not a statement
 * the semi colon(";") is missing.
 */
